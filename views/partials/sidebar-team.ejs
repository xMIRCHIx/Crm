<!-- Team Sidebar -->
<aside class="w-64 bg-slate-900 border-r border-slate-800 flex flex-col h-full flex-shrink-0">
  <!-- Brand Logo Section -->
  <div class="px-6 py-6 border-b border-slate-800 flex items-center justify-between">
    <div class="flex items-center space-x-3">
      <div class="w-8 h-8 rounded-lg bg-gradient-to-tr from-yellow-600 to-amber-400 flex items-center justify-center shadow-md">
        <i class="fa-solid fa-crown text-slate-950 font-bold"></i>
      </div>
      <div>
        <h1 class="text-lg font-bold text-slate-100 tracking-tight leading-none">Growlix</h1>
        <span class="text-[10px] uppercase font-bold tracking-widest text-[#C29B5C]">CRM Workspace</span>
      </div>
    </div>
  </div>

  <!-- Navigation Links -->
  <nav class="flex-1 px-4 py-6 space-y-1.5 overflow-y-auto">
    <!-- Dashboard -->
    <a href="/dashboard" class="flex items-center space-x-3 px-4 py-3 rounded-xl transition-all duration-200 <%= path === '/dashboard' ? 'bg-[#C29B5C]/10 text-[#C29B5C] font-semibold border-l-4 border-[#C29B5C]' : 'text-slate-400 hover:bg-slate-800/50 hover:text-slate-200' %>">
      <i class="fa-solid fa-chart-pie w-5"></i>
      <span class="text-sm">Dashboard</span>
    </a>

    <!-- Tasks -->
    <a href="/tasks" class="flex items-center space-x-3 px-4 py-3 rounded-xl transition-all duration-200 <%= path.startsWith('/tasks') ? 'bg-[#C29B5C]/10 text-[#C29B5C] font-semibold border-l-4 border-[#C29B5C]' : 'text-slate-400 hover:bg-slate-800/50 hover:text-slate-200' %>">
      <i class="fa-solid fa-list-check w-5"></i>
      <span class="text-sm">My Tasks</span>
    </a>

    <!-- Support Tickets -->
    <a href="/tickets" class="flex items-center space-x-3 px-4 py-3 rounded-xl transition-all duration-200 <%= path.startsWith('/tickets') ? 'bg-[#C29B5C]/10 text-[#C29B5C] font-semibold border-l-4 border-[#C29B5C]' : 'text-slate-400 hover:bg-slate-800/50 hover:text-slate-200' %>">
      <i class="fa-solid fa-headset w-5"></i>
      <span class="text-sm">Support Tickets</span>
    </a>
  </nav>

  <!-- Bottom Profile Summary -->
  <div class="p-4 border-t border-slate-800 bg-slate-900/50">
    <div class="flex items-center space-x-3">
      <div class="w-9 h-9 rounded-full bg-slate-800 flex items-center justify-center text-slate-300 font-bold border border-slate-700 uppercase">
        <%= user.name.charAt(0) %>
      </div>
      <div class="flex-1 min-w-0">
        <h4 class="text-xs font-semibold text-slate-200 truncate"><%= user.name %></h4>
        <span class="text-[10px] font-medium text-slate-500 truncate block"><%= user.email %></span>
      </div>
    </div>
  </div>
</aside>
